# GhostliAI Standalone Recovery Environment Configuration
# This is a clean environment file for the forked standalone instance

# Database Configuration - Standalone Instance
DATABASE_URL=postgres://ghostli_standalone_db

# JWT Configuration
JWT_SECRET=ghostliai-standalone-security-key-2025

# Session Configuration  
SESSION_SECRET=ghostliai-standalone-session-secret

# Application Configuration
NODE_ENV=production
PORT=5000

# Email Configuration (Optional - will be disabled if not provided)
# SENDGRID_API_KEY=your_sendgrid_key_here

# AI Integration (Optional - will be disabled if not provided)
# OPENAI_API_KEY=your_openai_key_here

# Payment Integration (Optional - will be disabled if not provided)
# PAYPAL_CLIENT_ID=your_paypal_client_id
# PAYPAL_CLIENT_SECRET=your_paypal_client_secret